{% set python_min = "3.10" %}
{% set version = "0.13.1" %}

package:
  name: lintquarto
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/l/lintquarto/lintquarto-{{ version }}.tar.gz
  sha256: c02b5a03dbb2f389bbcb8de3a87675c43720421442e869ab1b54e15d6dca5598

build:
  entry_points:
    - lintquarto = lintquarto.__main__:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - flit
    - pip
  run:
    - python >={{ python_min }}
    - toml
    - tree_sitter
    - tree-sitter-markdown
    - pyyaml

test:
  imports:
    - lintquarto
  commands:
    - pip check
    - lintquarto --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://lintquarto.github.io/lintquarto/
  summary: Package for running linters, formatters, static type checkers and code analysis tools on python code in quarto (.qmd) files.
  dev_url: https://github.com/lintquarto/lintquarto
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - amyheather
